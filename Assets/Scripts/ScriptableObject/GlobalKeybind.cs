using UnityEngine;

namespace ScriptableObject
{
    public class GlobalKeybind : UnityEngine.ScriptableObject
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
